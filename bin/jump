#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

ROOM=$($DIR/coordinates $1 $2 $3)

if [ ! $? -eq 0 ]; then
	echo "$ROOM"
	exit 1
fi


if [ -e "$ROOM" ]; then
	cd "$ROOM"
else
	echo "Location does not exist"
fi

